@using CKWMS.App_Code
@using CKWMS.Models
@using X.PagedList
@using X.PagedList.Mvc
@{
    ViewBag.Title = "商品注册证清单";
    string _searchcondition = ViewBag.SearchCondition;
}

<script type="text/javascript">
    $(document).ready(function () {
        var _zczid = $("#zczbh").val();
        $("#pl_guige").hide();
        $(".guige").click(function () {
            _zczid=$(this).parent().parent().find("input[type=checkbox]").val();
            getGuiGe(_zczid);
            $("#pl_guige").show();
        });
        $("#pl_close").click(function () {
            $("#pl_guige").hide();
        });
        $("#bt_guige_delete").click(function () {
            var _ggdel = "";
            $("#ggdatatable tr input[type=checkbox]").each(function () {
                if (this.checked) {
                    _ggdel = _ggdel + $(this).val() + ",";
                }
            });
            if (_ggdel.length > 0) {
                var _delok = confirm("确定要删除这些规格吗？");
                if (_delok) {
                    $.post(
                        "/base_zhucezhenggg/deletenow",
                        {
                            del: _ggdel
                        },
                        function (data) {
                            if (data == 1)
                                getGuiGe(_zczid);
                            else
                                alert("删除失败，请重试！");
                        }
                    );
                }
            }
        });
        $("#bt_guige_refresh").click(function () {
            getGuiGe(_zczid);
        });
        $("tr").bind("dblclick", function () {
            var u_id = $(this).find('td').eq(0).find('input').val();
            if (u_id != null) {
                var url_go = "/base_shangpinzcz/Edit/" + u_id;
                location.href = url_go;
            }
        });
    });
    function AddInfo() {
        location.href = "/base_shangpinzcz/Add";
    }
    function EditInfo() {
        $("#mydatatable tr input[type=checkbox]").each(function () {
            if (this.checked)
                location.href = "/base_shangpinzcz/Edit/" + $(this).val();
        });
    }
    function DelInfo() {
        var sDel = "";
        $("#mydatatable tr input[type=checkbox]").each(function () {
            if (this.checked)
                sDel = sDel + ", " + $(this).val()
        });
        if (sDel.length < 2)
            return;
        var url_go = "/base_shangpinzcz/Delete/?del=" + sDel;
        location.href = url_go;
    }
    function getGuiGe(zczid) {
        //var _zczid = $("#zczbh").val();
        var _zczid = zczid;
        var filltable = "";
        $.post(
            "/base_zhucezhenggg/getdetail",
            {
                zczbh: _zczid
            },
            function (data) {
                for (var i in data) {
                    filltable = filltable + '<tr><td><div class="checkbox"><label><input type="checkbox" value="';
                    filltable = filltable + data[i].ID;
                    filltable = filltable + '"></label></div></td><td>';
                    filltable = filltable + data[i].Guige;
                    filltable = filltable + '</td><td>';
                    filltable = filltable + data[i].Memo;
                    filltable = filltable + '</td><td>';
                    filltable = filltable + data[i].Makedate;
                    filltable = filltable + '</td><td>';
                    filltable = filltable + data[i].Makeman;
                    filltable = filltable + '</td></tr>';
                }
                $("#pl_guige tbody").html(filltable);
            });
    }

</script>
@{
    List<SearchConditionModel> scms = new List<SearchConditionModel>();
    if (_searchcondition == null || _searchcondition == "")
    {
        SearchConditionModel scm = new SearchConditionModel();
        scm.ItemCode = "Bianhao";
        scm.ItemTitle = "注册证编号";
        scm.ItemType = "System.String";
        scms.Add(scm);

        scm = new SearchConditionModel();
        scm.ItemCode = "Mingcheng";
        scm.ItemTitle = "商品名称";
        scm.ItemType = "System.String";
        scms.Add(scm);


    }
    else
    {
        string[] scs = _searchcondition.Split(';');
        if (scs.Length > 0)
        {
            SearchConditionModel scm = new SearchConditionModel();
            scm.ItemCode = "Bianhao";
            scm.ItemTitle = "注册证编号";
            scm.ItemType = "System.String";
            if (scs.Length > 1)
            {
                scm.ItemValue = scs[0].Split(',')[1];
                scm.ItemOpValue = scs[0].Split(',')[2];
                scm.ItemJion = scs[0].Split(',')[3];
            }
            scms.Add(scm);

            scm = new SearchConditionModel();
            scm.ItemCode = "Mingcheng";
            scm.ItemTitle = "商品名称";
            scm.ItemType = "System.String";
            if (scs.Length > 1)
            {
                scm.ItemValue = scs[1].Split(',')[1];
                scm.ItemOpValue = scs[1].Split(',')[2];
                scm.ItemJion = scs[1].Split(',')[3];
            }
            scms.Add(scm);


        }
    }
}

<div class="breadcrumbs" id="breadcrumbs">
    <script type="text/javascript">
        try { ace.settings.check('breadcrumbs', 'fixed') } catch (e) { }
    </script>
    <ul class="breadcrumb">
        <li>
            <i class="icon-home home-icon"></i>
            <a href="/Home">首页</a>
        </li>
        <li class="active">商品注册证</li>
    </ul>
</div>
<div class="page-content">
    @Html.Search_Condition((int)Session["user_id"], "商品注册证_条件组合查询", "/base_shangpinzcz/index", "base_shangpinzczindex", scms)
    <div class="page-header">
        <p>
            <button type="button" class="btn btn-default " id="bt_searchInfo" data-toggle="modal" data-target="#myModal"><i class="icon-search"></i>查询</button>
            <button type="button" class="btn btn-default " id="bt_addInfo" onclick="AddInfo()"><i class="icon-adn"></i>新增</button>
            <button type="button" class="btn btn-default " id="bt_updateInfo" onclick="EditInfo()"><i class="icon-edit"></i>编辑</button>
            <button type="button" class="btn btn-default " id="bt_deleteInfo" onclick="DelInfo()"><i class="icon-remove"></i>删除</button>
            <button type="button" class="btn btn-default " id="bt_refreshInfo" onclick="javascript: location.reload()"><i class="icon-refresh"></i>刷新</button>
        </p>
    </div>
    <div style="overflow:auto;">
        <div class="table-responsive">
            <table class="table" style="overflow: hidden;text-overflow:ellipsis;white-space: nowrap;" id="mydatatable">
                <caption></caption>
                <thead>
                    <tr>
                        <td></td>
                        <td>注册证编号</td>
                        <td>商品名称</td>
                        <td>规格</td>
                        <td>产品标准</td>
                        <td>注册证有效期</td>
                        @*<td>录入日期</td>*@
                        <td>中文编号</td>
                        <td>产地</td>
                        <td>生产企业</td>
                        <td>服务机构</td>
                        <td>注册证照片</td>
                        <td>输入人</td>
                        <td>备注</td>
                        @*<td>是否失效</td>
                        <td>适用范围</td>
                        <td>注册代理</td>*@
                       
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in ViewBag.base_shangpinzcz)
                {
                        <tr>
                            <td>
                                <div class="checkbox">
                                    <label><input type="checkbox" value="@item.ID"></label>
                                </div>
                            </td>
                            <td>@item.Bianhao</td>
                            <td>@item.Mingcheng</td>
                            @*规格*@
                            <td><button class="btn guige">浏览</button></td>
                            <td>@item.Bianzhun</td>
                            <td>@item.ZhucezhengYXQ.ToString("yyyy-MM-dd")</td>
                            @*<td>@item.MakeDate.ToString("yyyy-MM-dd")</td>*@
                            <td>@item.BianhaoZW</td>
                            <td>@item.Chandi</td>
                            <td>@Html.GetDataValue_ID("","名称",(int)item.ShengchanqiyeID)</td>
                            <td>@item.ShouhuoJG</td>
                            <td><a href="/files/zhengzhao/@item.ZhucezhengTP" target="_blank">浏览</a></td>
                            <td>@Html.GetDataValue_ID("userinfo", "全名", (int)item.MakeMan)</td>
                            <td>@item.Beizhu</td>
                            @*<td id="changeText">@item.ShixiaoSF</td>
                            <td>@item.ShiyongFW</td>
                            <td>@item.ZhuceDL</td>*@
                        </tr>
                }
                </tbody>
            </table>

            
            <div class="pager">
                @Html.PagedListPager((IPagedList)Model, page => Url.Action("Index", new { page }))
            </div>

        </div>
    </div>
</div>
<div id="pl_guige" class="panel panel-primary" style="position:absolute;top:100px;left:200px;width:600px;height:400px;z-index:999;">
    <div class="panel-heading">
        <h3 class="panel-title">规格管理<button id="pl_close" class="pull-right">X</button></h3>
    </div>
    <div class="panel-body">
        <div class="center">
            <button type="button" class="btn btn-default " id="bt_guige_delete"><i class="icon-remove"></i>删除</button>
            <button type="button" class="btn btn-default " id="bt_guige_refresh"><i class="icon-refresh"></i>刷新</button>
            <table class="table" id="ggdatatable">
                <caption></caption>
                <thead>
                    <tr>
                        <td></td>
                        <td>规格</td>
                        <td>说明</td>
                        <td>制单日期</td>
                        <td>制单人</td>
                    </tr>
                </thead>
                <tbody></tbody>
            </table>
        </div>
    </div>
</div>
